{
  "name": "anchorpoint",
  "version": "0.1.0",
  "private": true,
  "description": "We Need: reproduce the DeepSeek V4 Pro first-request 'We need' trajectory anchor (Minimal-aligned bootstrap, deferred skill catalog, CJK bridge, lab tooling)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check preset/tool-bootstrap.mjs && node --test"
  }
}
